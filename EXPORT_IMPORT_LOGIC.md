# 📄 Export/Import Logic Documentation

## 📝 Общее описание

Функционал Export/Import предназначен для резервного копирования и восстановления **метаданных ссылок** и **настроек приложения**, но **НЕ самих файлов изображений/видео**.

---

## 📦 Что экспортируется?

### 1. Метаданные ссылок (wallpapers)

Для каждой ссылки сохраняется:

```json
{
  "id": "example",              // ID ссылки
  "filename": "image.jpg",       // Имя файла на сервере
  "size": 1234567,               // Размер файла в байтах
  "created_at": "2026-02-25T...", // Дата создания
  "type": "image"                // Тип: image или video
}
```

**ВАЖНО:** Сохраняются **только ссылки на файлы**, а не сами файлы!

### 2. Настройки приложения (settings)

```json
{
  "lang": "en",                  // Текущий язык
  "theme": "dark",               // Тема (dark/light)
  "viewMode": "list",            // Режим просмотра (list/grid)
  "sortBy": "date_desc"          // Сортировка
}
```

### 3. Метаинформация

```json
{
  "version": "1.0.0",           // Версия формата экспорта
  "exportDate": "2026-02-25T..." // Дата экспорта
}
```

---

## 🔒 Что НЕ экспортируется?

### ❌ Файлы изображений и видео

**Почему?**
1. **Размер файлов**: Изображения могут весить много (MB/GB), JSON файл станет огромным
2. **Физическое хранение**: Файлы хранятся на сервере в папке `uploads/`
3. **Связь с ID**: При удалении ссылки файл удаляется с диска

**Поэтому:**
- Экспорт сохраняет **только список ссылок**
- Импорт восстанавливает **структуру ссылок**
- Файлы нужно резервировать отдельно (например, через backup папки `uploads/`)

---

## 📂 Структура экспортированного JSON

```json
{
  "version": "1.0.0",
  "exportDate": "2026-02-25T16:30:00.000Z",
  "settings": {
    "lang": "en",
    "theme": "dark",
    "viewMode": "list",
    "sortBy": "date_desc"
  },
  "wallpapers": [
    {
      "id": "sunset",
      "filename": "sunset_1234567890.jpg",
      "size": 2456789,
      "created_at": "2026-02-20T10:00:00.000Z",
      "type": "image"
    },
    {
      "id": "ocean-video",
      "filename": "ocean_9876543210.mp4",
      "size": 15678900,
      "created_at": "2026-02-21T14:30:00.000Z",
      "type": "video"
    }
  ]
}
```

---

## ⚡ Логика экспорта (Export)

### Шаг 1: Сбор данных

```javascript
const exportData = {
    version: '1.0.0',
    exportDate: new Date().toISOString(),
    settings: {
        lang: STATE.lang,                      // Из localStorage
        theme: localStorage.getItem('theme'),   // dark/light
        viewMode: STATE.viewMode,               // list/grid
        sortBy: STATE.sortBy                    // Текущая сортировка
    },
    wallpapers: STATE.wallpapers  // Весь массив ссылок
};
```

### Шаг 2: Конвертация в JSON

```javascript
const dataStr = JSON.stringify(exportData, null, 2);
// null, 2 - для красивого форматирования
```

### Шаг 3: Создание Blob и скачивание

```javascript
const dataBlob = new Blob([dataStr], { type: 'application/json' });
const url = URL.createObjectURL(dataBlob);
const link = document.createElement('a');
link.href = url;
link.download = `lanpaper-backup-${new Date().toISOString().split('T')[0]}.json`;
// Пример имени: lanpaper-backup-2026-02-25.json

document.body.appendChild(link);
link.click();  // Автоматическое скачивание
document.body.removeChild(link);
URL.revokeObjectURL(url);
```

---

## ⚙️ Логика импорта (Import)

### Шаг 1: Чтение файла

```javascript
const text = await file.text();
const data = JSON.parse(text);
```

### Шаг 2: Валидация

```javascript
if (!data.wallpapers || !Array.isArray(data.wallpapers)) {
    throw new Error('Invalid data format');
}
```

Проверяем:
- Наличие `wallpapers`
- `wallpapers` является массивом

### Шаг 3: Подтверждение пользователя

```javascript
const count = data.wallpapers.length;
const confirmMsg = `Import ${count} links? This will replace current data.`;

if (!confirm(confirmMsg)) {
    return;  // Пользователь отменил
}
```

**ВАЖНО:** Импорт **ЗАМЕНЯЕТ** текущие данные!

### Шаг 4: Восстановление настроек

```javascript
if (data.settings) {
    // Язык
    if (data.settings.lang) {
        await setLanguage(data.settings.lang);
    }
    
    // Тема
    if (data.settings.theme) {
        STATE.isDark = data.settings.theme === 'dark';
        localStorage.setItem('theme', data.settings.theme);
        applyTheme();
    }
    
    // Режим просмотра
    if (data.settings.viewMode) {
        STATE.viewMode = data.settings.viewMode;
        localStorage.setItem('viewMode', data.settings.viewMode);
        applyViewMode(data.settings.viewMode);
    }
    
    // Сортировка
    if (data.settings.sortBy) {
        STATE.sortBy = data.settings.sortBy;
        localStorage.setItem('sortBy', data.settings.sortBy);
        if (DOM.sortSelect) DOM.sortSelect.value = data.settings.sortBy;
    }
}
```

### Шаг 5: Восстановление ссылок

```javascript
STATE.wallpapers = data.wallpapers;
filterAndSort();  // Обновляем отображение
```

---

## ⚠️ Важные особенности

### 1. Файлы остаются на сервере

После импорта:
- ✅ Восстанавливаются ссылки (ID)
- ✅ Восстанавливаются метаданные (filename, size, type)
- ⚠️ Файлы должны уже существовать в `uploads/` с теми же именами!

**Пример сценария:**

1. У вас есть ссылка `sunset` с файлом `sunset_1234567890.jpg`
2. Вы делаете Export → сохраняется JSON с метаданными
3. Вы удаляете ссылку `sunset` → файл `sunset_1234567890.jpg` **удаляется с диска**
4. Вы делаете Import → ссылка `sunset` восстанавливается
5. Но файла нет → будет отображаться "No image"

**Решение:** Резервное копирование папки `uploads/` отдельно!

### 2. Рекомендуемая стратегия backup

#### Полный backup:
```bash
# 1. Резервное копирование файлов
cp -r uploads/ backup/uploads_2026-02-25/

# 2. Скачать JSON через Export
cp ~/Downloads/lanpaper-backup-2026-02-25.json backup/
```

#### Восстановление:
```bash
# 1. Восстановить файлы
cp -r backup/uploads_2026-02-25/* uploads/

# 2. Загрузить JSON через Import
```

### 3. Замена данных

Импорт **ПОЛНОСТЬЮ ЗАМЕНЯЕТ** текущие данные:
- ❌ Старые ссылки удаляются
- ✅ Новые ссылки загружаются

**Внимание:** Появляется подтверждение:
> "Import 15 links? This will replace current data."

---

## 📊 Сравнение с другими подходами

| Метод | Размер | Включает файлы | Скорость | Использование |
|---------|----------|-------------------|------------|------------------|
| **JSON Export** | ~10 KB | ❌ Нет | ⚡ Мгновенно | Быстрый backup метаданных |
| **ZIP Archive** | ~500 MB | ✅ Да | 🐌 Медленно | Полный backup с файлами |
| **Database Dump** | ~50 KB | ❌ Нет | ⚡ Быстро | Для SQL баз данных |
| **uploads/ Copy** | ~500 MB | ✅ Да | 🐌 Медленно | Резервное копирование папки |

**Лучшая практика:** Использовать **оба метода**:
1. JSON Export - для метаданных (ежедневно)
2. `uploads/` Copy - для файлов (еженедельно)

---

## 🛠️ Use Cases

### 1. Миграция на новый сервер

```bash
# Старый сервер
1. Export JSON
2. Скопировать uploads/

# Новый сервер
3. Загрузить uploads/
4. Import JSON
```

### 2. Резервное копирование перед обновлением

```bash
# Перед обновлением
1. Export JSON
2. cp -r uploads/ uploads_backup/

# После обновления (если что-то сломалось)
3. cp -r uploads_backup/* uploads/
4. Import JSON
```

### 3. Тестовая среда

```bash
# Копирование данных из production в staging
1. Production: Export JSON
2. Production: cp -r uploads/ staging_uploads/
3. Staging: Import JSON
```

---

## 📝 Пример использования

### Сценарий: У вас 5 ссылок с изображениями

**До экспорта:**
```
uploads/
  ├── sunset_1234567890.jpg     (2.5 MB)
  ├── ocean_9876543210.mp4      (15 MB)
  ├── mountain_1111111111.jpg   (3.2 MB)
  ├── forest_2222222222.jpg    (1.8 MB)
  └── sky_3333333333.jpg        (2.1 MB)

Всего: ~24.6 MB файлов
```

**После Export:**
```json
lanpaper-backup-2026-02-25.json  (~5 KB)

{
  "wallpapers": [
    {"id": "sunset", "filename": "sunset_1234567890.jpg", ...},
    {"id": "ocean-video", "filename": "ocean_9876543210.mp4", ...},
    {"id": "mountain", "filename": "mountain_1111111111.jpg", ...},
    {"id": "forest", "filename": "forest_2222222222.jpg", ...},
    {"id": "sky", "filename": "sky_3333333333.jpg", ...}
  ]
}
```

**Разница:**
- JSON: 5 KB vs Файлы: 24.6 MB
- Экспорт в **5000 раз меньше**!

---

## ❓ FAQ

### Q: Почему не экспортируются файлы?
**A:** Чтобы избежать огромных JSON файлов. Файлы лучше резервировать отдельно.

### Q: Что случится, если файла нет после импорта?
**A:** Ссылка восстановится, но будет показывать "No image". Нужно восстановить файл в `uploads/`.

### Q: Можно ли импортировать часть данных?
**A:** Нет, импорт заменяет все данные. Для частичного восстановления нужно редактировать JSON.

### Q: Как часто делать backup?
**A:** Рекомендации:
- JSON Export: ежедневно или перед важными изменениями
- `uploads/` Copy: еженедельно

### Q: Можно ли автоматизировать?
**A:** Да! Можно создать cron задачу:
```bash
# Каждый день в 3:00
0 3 * * * cp -r /path/to/lanpaper/uploads /path/to/backup/uploads_$(date +\%Y-\%m-\%d)
```

---

## 🔗 Ссылки

- [IMPROVEMENTS.md](./IMPROVEMENTS.md) - Полный список улучшений
- [export-import.js](./static/js/export-import.js) - Исходный код
- [GitHub Lanpaper](https://github.com/zoixc/lanpaper)

---

**Вывод:** Export/Import предназначен для **метаданных** и **настроек**. Файлы нужно резервировать отдельно! 💾
