# Исправления в выпадающем меню

## Проблемы и решения

### 1. 🌐 Отсутствуют кнопки языков

**Проблема**: В `settings-menu.js` был код, который ожидал глобальную переменную `LANGS` из `app.js`, но она не была экспортирована.

**Решение**:
```javascript
// Теперь в settings-menu.js есть собственный список языков:
const LANGS = {
    'en': 'EN',
    'ru': 'RU',
    'de': 'DE',
    'fr': 'FR',
    'it': 'IT',
    'es': 'ES'
};

// Кнопки создаются немедленно при инициализации
Object.keys(LANGS).forEach(code => {
    const btn = document.createElement('button');
    btn.className = 'lang-option';
    btn.textContent = LANGS[code];
    // ...
});
```

**Результат**: ✅ Теперь кнопки EN, RU, DE, FR, IT, ES отображаются в сетке 3x2

---

### 2. 🔘 Странная иконка меню ("крест с чем-то")

**Проблема**: Иконка settings была сложной и некорректно рендерилась.

**Старая иконка** (проблемная):
```svg
<svg viewBox="0 0 24 24">
  <circle cx="12" cy="12" r="3"/>
  <path d="M12 1v6m0 6v6m9-9h-6m-6 0H3m15.364 6.364l-4.243-4.243m0 0l-4.242-4.242m4.242 4.242l-4.242 4.243m4.242-4.243l4.243 4.243"/>
</svg>
```

**Новая иконка** (чистая шестеренка):
```svg
<svg width="18" height="18" viewBox="0 0 24 24">
  <circle cx="12" cy="12" r="3"></circle>
  <path d="M12 1v6m0 6v6"></path>
  <path d="M17 12h6M1 12h6"></path>
  <path d="m4.2 19.8 4.2-4.2m7.6-7.6 4.2-4.2"></path>
  <path d="m19.8 19.8-4.2-4.2M7.8 7.8 3.6 3.6"></path>
</svg>
```

**Результат**: ✅ Чистая иконка шестеренки с анимацией вращения (45°)

---

### 3. 📁 Лишняя кнопка "Выберите файл"

**Проблема**: `<input type="file">` для импорта отображался как видимая кнопка.

**Было**:
```html
<button id="importBtn">Import</button>
<input id="importFile" type="file" accept=".json">
<!-- input был виден как "Выберите файл" -->
```

**Стало**:
```html
<button id="importBtn">Import</button>
<input id="importFile" type="file" accept=".json" style="display:none;" aria-hidden="true">
<!-- input скрыт -->
```

**Результат**: ✅ Только две кнопки: Export и Import

---

### 4. 🔗 Экспорт `setLanguage` для settings-menu.js

**Проблема**: `settings-menu.js` не мог вызвать `setLanguage()` из `app.js`.

**Решение** в `app.js`:
```javascript
// Export setLanguage to window for settings-menu.js
window.setLanguage = setLanguage;

async function setLanguage(lang) {
    STATE.lang = lang;
    localStorage.setItem('lang', lang);
    // ...
}
```

**Результат**: ✅ Кнопки языков теперь переключают язык и обновляют интерфейс

---

## Дополнительные улучшения

### 🎨 CSS улучшения

```css
/* Анимация вращения иконки */
.settings-dropdown.open #settingsBtn svg {
    transform: rotate(45deg);
}

#settingsBtn svg {
    transition: transform .3s cubic-bezier(0.34, 1.56, .64, 1);
}

/* Активная кнопка языка */
.lang-option.active {
    background: var(--btn-primary);
    color: var(--bg);
    border-color: var(--btn-primary);
    font-weight: 700;
}
```

### ♻️ Fallback для незагруженного app.js

```javascript
// Если app.js еще не загрузился
if (typeof window.setLanguage === 'function') {
    await window.setLanguage(code);
} else {
    // Fallback: сохраняем в localStorage и перезагружаем
    localStorage.setItem('lang', code);
    location.reload();
}
```

---

## Тестирование

### Проверьте следующее:

- [ ] Открыть admin.html
- [ ] Нажать на иконку шестеренки (она должна повернуться на 45°)
- [ ] Увидеть раздел "LANGUAGE" с 6 кнопками: EN, RU, DE, FR, IT, ES
- [ ] Увидеть раздел "DATA" с 2 кнопками: Export и Import
- [ ] Не должно быть кнопки "Выберите файл"
- [ ] Переключить язык на RU (интерфейс должен обновиться)
- [ ] Проверить, что активная кнопка языка выделена (яркий фон, жирный шрифт)
- [ ] Нажать Export (должен скачаться JSON)
- [ ] Нажать Import (должен открыться файловый диалог)

---

## Итоговые изменения

### Файлы:
- ✅ `static/js/settings-menu.js` - добавлены кнопки языков, fallback
- ✅ `static/js/app.js` - экспорт `window.setLanguage`
- ✅ `admin.html` - исправлена иконка SVG, скрыт input[type=file]

### Результат:
✅ **6 кнопок языков** (EN, RU, DE, FR, IT, ES)  
✅ **Чистая иконка шестеренки** с анимацией  
✅ **Только 2 кнопки** в секции DATA (Export, Import)  
✅ **Работающее переключение языка**  
✅ **Все функции сохранены**  

---

**Дата**: 25 февраля 2026  
**Commit**: [311d3f3](https://github.com/zoixc/lanpaper/commit/311d3f303beb13672d4364a82ed7f1488653e462)
